server:
  port: 8082
 
spring:
  application:
    name: coach
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefix: config
        default-context: application
        profile-separator: '::'
        format: YAML
